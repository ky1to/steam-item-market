version: "3.9"
services:
  postgres_db:
    image: postgres
    restart: always
    environment:
      POSTGRES_USER: ${POSTGRES_USER}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD}
      POSTGRES_DB: ${POSTGRES_DB}
    ports:
      - "5435:5432"
    volumes:
      - data:/var/lib/postgresql/data

  postgres_user_db:
    image: postgres
    restart: always
    environment:
      POSTGRES_USER: ${AUTH_POSTGRES_USER}
      POSTGRES_PASSWORD: ${AUTH_POSTGRES_PASSWORD}
      POSTGRES_DB: ${AUTH_POSTGRES_DB}
    ports:
      - "5436:5432"
    volumes:
      - user_data:/var/lib/postgresql/data

  pgadmin:
    image: dpage/pgadmin4
    restart: always
    environment:
      PGADMIN_DEFAULT_EMAIL: ${PGADMIN_DEFAULT_EMAIL}
      PGADMIN_DEFAULT_PASSWORD: ${PGADMIN_DEFAULT_PASSWORD}
    ports:
      - "5053:80"
    depends_on:
      - postgres_db
      - postgres_user_db

  update_serves:
    build: 
      context: .
      dockerfile: Dockerfile-update_serves
    restart: always
    depends_on:
      - postgres_db
    volumes:
      - ./src/update_serves/logs:/app/logs

  api:
    build:
      context: .
      dockerfile: Dockerfile-api
    restart: always
    command: uvicorn main:app --host 0.0.0.0 --port 8000 --no-access-log
    ports:
      - "8000:8000"
    depends_on:
      - postgres_db
      - redis
    volumes:
      - ./src/api/logs:/app/logs

  web:
    build: ./src/nginx
    restart: always
    ports:
      - "80:80"
    depends_on:
      - api
      
  redis:
    restart: always
    image: redis:6-alpine
    ports:
      - "6381:6379"

volumes:
  data:
  user_data: